# Assumptions

- Fallback rent uses market-specific rent-per-sqft config.
- Taxes default to a configured annual tax percent if not provided.
- Valuation uses trimmed/median sale-comparable ppsf.
- Risk score is normalized to `[0,1]` from configurable weighted components.
- Advisory and ranking scores are not final decisions.
